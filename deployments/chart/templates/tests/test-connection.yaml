apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "admission-webhook.fullname" . }}-test-connection"
  labels:
{{ include "admission-webhook.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "admission-webhook.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
